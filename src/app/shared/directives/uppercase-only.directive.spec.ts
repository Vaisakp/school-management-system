import { UppercaseOnlyDirective } from './uppercase-only.directive';

describe('UppercaseOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new UppercaseOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
